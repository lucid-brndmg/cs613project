apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
  namespace: smp
data:
  01-tables.sql: |
    create table t_user
    (
        uid          bigserial  not null constraint user_pk primary key,
        username     text                                             not null constraint username unique,
        time_created timestamp                                        not null,
        time_login   timestamp                                        not null
    );

    create table t_user_follow (
        uid_source bigserial not null,
        uid_target bigserial not null,
        time_created timestamp not null,
        constraint user_follow_pk primary key (uid_source, uid_target)
    );

    create table t_post (
        pid bigserial not null constraint post_pk primary key,
        uid bigint not null,
        content text not null,
        time_created timestamp not null,
        parent_pid bigint,
        reference_pid bigint,
        like_count int default 0,
        repost_count int default 0,
        reply_count int default 0,
        likers_bf bytea
    );

    create table t_post_interaction (
        id bigserial not null constraint interaction_pk primary key,
        pid bigint not null,
        uid bigint not null,
        time_created timestamp not null,
        constraint unique_interaction unique (pid, uid)
    );
  02-seed.sql: |
    -- Dummy Users
    INSERT INTO t_user (username, time_created, time_login) VALUES
    ('alice', NOW(), NOW()),
    ('bob', NOW(), NOW()),
    ('charlie', NOW(), NOW()),
    ('david', NOW(), NOW()),
    ('eve', NOW(), NOW());

    -- Dummy Posts
    -- Assuming UIDs 1-5 for the above users
    INSERT INTO t_post (uid, content, time_created) VALUES
    (1, 'Just setting up my twitter clone!', NOW() - INTERVAL '1 day'),
    (1, 'Loving the new features.', NOW() - INTERVAL '1 hour'),
    (2, 'Hello world! Bob is here.', NOW() - INTERVAL '2 days'),
    (2, 'Anyone want to grab coffee?', NOW() - INTERVAL '30 minutes'),
    (3, 'Charlie checking in. This app is smooth.', NOW() - INTERVAL '5 hours'),
    (4, 'David here. Testing the feed.', NOW() - INTERVAL '3 days'),
    (5, 'Eve is watching...', NOW() - INTERVAL '1 minute');

    -- Dummy Follows
    -- Alice follows Bob and Charlie
    INSERT INTO t_user_follow (uid_source, uid_target, time_created) VALUES
    (1, 2, NOW()),
    (1, 3, NOW());

    -- Bob follows Alice
    INSERT INTO t_user_follow (uid_source, uid_target, time_created) VALUES
    (2, 1, NOW());

    -- Charlie follows Alice, Bob, David
    INSERT INTO t_user_follow (uid_source, uid_target, time_created) VALUES
    (3, 1, NOW()),
    (3, 2, NOW()),
    (3, 4, NOW());

    -- Eve follows everyone
    INSERT INTO t_user_follow (uid_source, uid_target, time_created) VALUES
    (5, 1, NOW()),
    (5, 2, NOW()),
    (5, 3, NOW()),
    (5, 4, NOW());
