apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-secret
  namespace: smp
type: Opaque
stringData:
  # Replace with your actual Cloudflare Tunnel token
  # Get this from: https://one.dash.cloudflare.com/ > Access > Tunnels
  tunnel-token: "YOUR_CLOUDFLARE_TUNNEL_TOKEN_HERE"
